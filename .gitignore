__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local runtime data (databases, HTTP caches). The imports directory is
# tracked so 'sync csv' has somewhere to look on a fresh checkout.
data/*
!data/imports/
data/imports/*
!data/imports/.gitkeep

# Local user configuration (the examples in config/ are tracked)
config/league.yaml
config/sources.yaml

# Web UI build artifacts
web/node_modules/
web/dist/
web/*.tsbuildinfo
